# Единственное место, где перечислены зависимости.
# pyproject.toml подтягивает этот файл через dynamic-секцию, Dockerfile ставит
# его напрямую - иначе списки в двух местах неизбежно разъезжаются.
aiogram>=3.15,<4
asyncpg>=0.30
# Шифрование анкеты в базе: паспортные данные и адреса открытым текстом
# в bot.users лежать не должны.
cryptography>=42
# Сборка договора в PDF. Чистый Python, но кириллице нужен внешний шрифт -
# Dockerfile ставит fonts-dejavu-core.
fpdf2>=2.7
